[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "openprism-fusion"
version = "0.4.0.dev0"
description = "Evidence-preserving visible/thermal fusion and UAV terrain mapping for models and operators"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
license-files = ["LICENSE"]
authors = [{name = "Alvaro Infante Flores"}]
keywords = ["sensor-fusion", "thermal", "computer-vision", "uav", "mapping"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Intended Audience :: Science/Research",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Topic :: Scientific/Engineering :: Artificial Intelligence",
  "Topic :: Scientific/Engineering :: GIS",
]
dependencies = [
  "numpy>=2.2,<3",
  "Pillow>=11,<13",
]

[project.optional-dependencies]
pixhawk = ["pymavlink==2.4.49"]
learned = ["torch>=2.4,<3"]
evaluation = ["torch>=2.4,<3", "torchvision>=0.19,<1"]
dev = ["build>=1.2", "jsonschema>=4.23", "pytest>=8.3,<10"]

[project.scripts]
openprism = "openprism.__main__:main"
openprism-train-egtcf = "openprism.learning.training:main"
openprism-evaluate-egtcf = "openprism.learning.evaluation:main"
openprism-evaluate-llvip-detection = "openprism.learning.detection_evaluation:main"
openprism-msrs-segmentation-probe = "openprism.learning.segmentation_probe:main"
openprism-caltech-segmentation-probe = "openprism.learning.caltech_segmentation_probe:main"

[project.urls]
Documentation = "https://github.com/Alvaroinfantee/openprism#readme"
Issues = "https://github.com/Alvaroinfantee/openprism/issues"
Repository = "https://github.com/Alvaroinfantee/openprism"

[tool.setuptools]
include-package-data = true

[tool.setuptools.packages.find]
include = ["openprism*"]
exclude = ["tests*", "tooling*"]

[tool.setuptools.package-data]
openprism = [
  "docs/*.md",
  "learning/*.py",
  "policy/*.json",
  "spec/*.json",
  "web/*.html",
  "web/*.css",
  "web/*.js",
]
